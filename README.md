# botctl

tg-bot control panel

## KDL configuration

- Runtime config used by the app: `config/serverbot.kdl`
- Extended example with command declarations and ACL bootstrap grants: `config/example.kdl`
