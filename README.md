# botctl

tg-bot control panel

## KDL configuration

- Runtime config used by the app: `config/serverbot.kdl`
- Production command declarations used by bot startup: all `commands/*.kdl` except `commands/example.kdl`
- Minimal parser sample config: `commands/example.kdl`
